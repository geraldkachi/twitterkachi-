import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    justifyContent: "space-between",
    marginHorizontal: 10,
  },
  number: {
    color: "grey",
    marginLeft: 5,
  },
  iconContanier: { flexDirection: "row", alignItems: "center" },
});

export default styles;
